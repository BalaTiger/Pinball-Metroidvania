# 阿刻戎轨道城：大型地图扩展规划

## 区域推进

地图以静滞核心为起点，沿东缘与南侧形成两条回环，最终在零潮静海汇合。每个区域由房间、连接走廊和 640×640 区块组成，区域数据同时携带生物群系、推荐强度和剧情钩子。

| 区域 | 叙事功能 | Boss |
| --- | --- | --- |
| 静滞核心 | 唤醒引力锚、拼合坐标 | 熔脉巨像等核心守卫 |
| 东缘风暴井 / 离子温室 | 取得导航棱镜、稳定零潮 | 授粉母体 |
| 深渊升降井 / 沉没环城 | 追查被抹除的王庭航线 | 环城摄政 |
| 苍穹桥列 / 回声骨库 | 找回载体身份残响 | 普通守卫与路线资源 |
| 逆光观测站 | 校准零潮入口 | 逆光观测者 |
| 零潮静海 / 空洞冠冕 | 完成最终弹射并打破循环 | 空洞冠冕（零相） |

## 运行时策略

- `src/world.ts` 保存完整的作者几何，但 `loadedChunks()` 只返回玩家周围 3×3 区块。
- `Game.activeGeometry` 在跨区块时刷新；弹射障碍、敌人碰撞、丢弃位置检测和实体绘制都使用该几何。
- `inWalkable()` 保留完整世界连通性，避免寻路在区块边界产生断点。
- 星图显示 640 网格、区域边界、当前区块、已探索单元和已记录折跃点。

## 主线与遭遇

`src/lore.ts` 将区域背景、信号、目标和五个主线节点分离为数据；进入区域、HUD 目标卡、标题页和星图图例共享同一套文本。`src/bosses.ts` 以数据驱动九名 Boss，统一管理三阶段血量阈值、移动行为、终阶伤害和掉落描述，旧存档启动时由 `ensureBossRoster()` 自动补齐新遭遇。
